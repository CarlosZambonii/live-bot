services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: backseat
      POSTGRES_PASSWORD: backseat
      POSTGRES_DB: backseat
    ports: ["5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
volumes:
  pgdata:
